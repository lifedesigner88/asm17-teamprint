{
  "name": "persona-mirror",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "nx": "nx",
    "dev:frontend": "nx run frontend:dev",
    "dev:backend": "nx run backend:dev",
    "dev": "nx run-many -t dev -p frontend,backend"
  },
  "devDependencies": {
    "nx": "^20.7.0"
  }
}

