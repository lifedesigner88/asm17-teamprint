# 2026-03-25

## Day Summary

- Repositioned the root domain from a dashboard-first ASM17 tool entry into a Sejong-first PR entry aimed at fast shared-link persuasion.
- Reordered the first-run experience so `Creator PR`, `AI Sejong`, YouTube, and the built dashboard now read as proof assets instead of side surfaces.
- Split the Sejong surface into `Park Sejong PR` and a separate `AI Sejong` chat page so PR and chat no longer compete inside one route.
- Removed the separate root landing again and made `/` render `Park Sejong PR` directly, while folding the older `/persona/sejong` link back into the same entry.
- Moved the Seoul dashboard nav item near the bottom, pushed the disabled Busan dashboard slot to the very bottom, and shortened its disabled badge copy to a lighter planned-state label.
- Replaced the sidebar tagline text with a clickable Naver Blog card, settled it at the very bottom of the sidebar, later compressed its English label down to a single `Blog` wordmark, and localized the Korean label to `블로그`.
- Added the project GitHub repository as a first-class external link in the sidebar action area, later moved it to the very bottom, simplified it so the card no longer shows the repo slug, and finally renamed the visible label to `PR 프로젝트`.
- Rebuilt the three sidebar action links into one aligned card pattern, added a KakaoTalk-style icon treatment, and then toned the icon badges down so `1:1 문의`, `Blog`, and `PR 프로젝트` read as one deliberate bundle without stealing focus from the main nav.
- Rebalanced the sidebar spacing and nav hierarchy again so the brand block sits lower, the card stack breathes more evenly, and `세종AI 챗봇` now reads as the clearest primary nav item.
- Removed the redundant `박세종 PR` nav button because the top brand card already serves as the home/root entry.
- Started the root-page refactor by lifting the main career-transition hero card to the very top, ahead of the shorter intro and supporting cards.
- Simplified the top hero actions by removing the duplicate GitHub chip and promoting the Notion link into a larger right-side portfolio button.
- Removed the hero `핵심 가치` chip row so the opening view stays focused on the positioning copy and portfolio CTA.
- Pulled the portfolio CTA back out from the hero itself and rebuilt the next row as a paired `최종면접 포트폴리오` + `AI 제작 메모` support band directly under the first card.
- Rewrote the hero message itself away from the harder `Founder-PM` phrasing into a clearer story about moving from helping students find jobs to building jobs directly.
- Moved the `휴포지토리 보기` button out of the intro copy block and into the `AI 제작 메모` card so the proof links stay grouped together.
- Removed the extra hero subheadline line so the top card now ends on the shorter self-introduction without repeating the same point twice.
- Restyled the Notion badge to a white background with a black icon and bumped its size slightly so it feels closer to Notion's visual identity.
- Moved the `with Codex GPT 5.4` signature up next to `휴포지토리 보기` so the AI note card ends with one compact action row.
- Removed the separate `실전 팀빌딩 PR` intro card altogether so the root page no longer repeats an extra explanatory block after the hero support row.
- Removed the `함께 만들 방향` card as well, so the root page now skips directly from the top proof band into the next substantive sections.
- Removed the creator-side `아이덴티티 타임라인` section too, so the root page keeps moving instead of pausing on a long retrospective block.
- Removed the empty bottom padding from the creator hero card by skipping its unused content block on the root page.
- Added a short plain-language SDGs explanation under the section heading so first-time visitors do not have to already know the term.
- Added a circular SDG wheel link in the SDGs header that opens the official UN page for all 17 goals at once, with a local fallback icon if the remote asset fails.
- Restored some bottom padding inside the creator hero card after that cleanup so the `공개 페르소나` block does not feel visually cramped.
- Localized the teammate role labels into Korean and removed the word `오너` from those titles so the tone feels lighter and less burdensome.
- Added the `React Router v7` preference into the frontend teammate-role bullets so the collaboration expectation is clearer up front.
- Added a playful mini health-check button to the right side of the creator hero header that measures `/health` latency and prints the response time inline.
- Trimmed one frontend teammate-role bullet so the expectations stay shorter and less prescriptive.
- Rebuilt the `연락` section into a desktop two-column layout with the mail-copy action living in its own right-side card, while letting mobile fall back to a vertical stack.
- Reworked the Notion portfolio card into a simpler centered layout by dropping the extra `Notion` label, removing the shortcut arrow, and keeping the icon-title pair centered.
- Refined the SDGs section so the header explanation, official all-goals link, and individual goal cards now feel like one coherent card family instead of a plain text block plus a floating wheel icon.
- Softened the SDGs explainer again by removing the extra card-like shading, widening the text line, and shortening the Korean all-goals link label to `17개 목표 보기`.
- Shortened the SDGs AI note down to a simple Hupository-based inference line so the footer reads faster and less defensively.
- Shortened the MBTI AI note to the same Hupository-based wording so the predictive helper notes now feel consistent across sections.
- Rewrote the contact CTA copy so it now directly invites interested teammates to reach out by email and explains why email is the preferred working-style channel.
- Swapped the contact CTA wording from `현업에서는` to `실무에서는` so the sentence sounds a bit more natural and grounded in Korean.
- Reframed the old `지금` card into `앞으로의 계획`, changing it from urgency copy into a learning-first team mindset and simple 2026–2027 startup timeline.
- Tightened that `앞으로의 계획` copy again so the preference now lands more clearly on learning posture and valuing even a one-hour conversation with working experts.
- Retitled that same card once more around valuing meetings with working mentors, and shortened the supporting line into a more direct preference for teammates who want to learn from mentors and experts.
- Rewrote the `제가 맡을 역할` card into a `서포트형 리더십` framing centered on building jobs people want to go to, prioritizing teammates' desired roles, and backing the team by learning missing functions.
- Added a small extra indent to the `앞으로의 계획` timeline bullets so those three date lines sit a bit more comfortably inside the card.
- Matched that same slight bullet indent on the `서포트형 리더십` card so the two lower cards now share the same list rhythm.
- Renamed the old `앞으로의 계획` label to the shorter `선호 팀원 유형` so the card title now matches its actual content more directly.
- Shortened the teammate-preference title once more to `멘토님의 가치를 아시는 분` so the card reads faster at a glance.
- Added a deliberate line break to the contact CTA heading and made the card title preserve newlines so the message now splits cleanly across two lines.
- Shifted the `세종AI 챗봇` nav card from the old emerald accent into a softer rose-red accent while keeping the same featured-card structure and emphasis.
- Removed the extra hero-style intro card from the dedicated `세종AI 챗봇` page so the route now lands directly on the chat experience itself.
- Rewrote the remaining Q&A/chat-copy labels so they now present the experience more directly as `휴포지토리 기반 세종 AI` instead of a generic persona-conversation surface.
- Added the `✅ 휴포지토리 보기` action to the dedicated Sejong AI chat page as a right-aligned utility button so the underlying source repo stays one click away there too.
- Moved that Hupository action into the Sejong AI card itself, aligning it inside the card header instead of leaving it floating above the chat surface.
- Removed the now-redundant Hupository-based chat description line and made the card hide empty descriptions cleanly instead of leaving visual gaps.
- Expanded the `작동 방식` developer card with real implementation detail: the server reads local Hupository YAML/README files derived from the GitHub repo and keyword-selects which snippets get injected into the prompt.
- Retinted the chat logging notice into a rose/red warning tone so the “questions and answers are being logged” message reads with clearer caution.
- Added a simple model note to the `작동 방식` modal, explicitly naming Anthropic Claude Haiku 4.5 and its current model ID for developer curiosity.
- Updated README and changelog docs so the repo now describes the same front-door hierarchy the app shows.

## Recommended Reading Order

1. Root routing and navigation hierarchy
2. Root landing copy and CTA structure
3. Share metadata and OG asset
4. README and changelog sync

## 1. Root Entry Was Collapsed Into Park Sejong PR

### What Changed

- Removed the old `/ -> /seoul/dashboard` redirect and, later, removed the separate root landing itself.
- `/` now renders `Park Sejong PR` directly, `/home` redirects to `/`, and the older `/persona/sejong` path folds back into the root entry.
- Cleaned the app shell navigation so `Park Sejong PR`, `AI Sejong`, and the dashboard links read in a more deliberate order, with Seoul above the final disabled Busan slot.
- Swapped the old sidebar explanatory sentence out for a clickable Naver Blog card with a brand-colored icon treatment and a final resting place at the bottom action area, then simplified the visible text so English shows `Blog` while Korean shows `블로그`.
- Added a dedicated project GitHub card in the same sidebar action cluster, then simplified it to a cleaner single-line label without the visible repo slug.
- Unified the sidebar action area itself so all three external links now share the same icon-left card layout, spacing, and alignment, with smaller and softer icon badges that feel more supportive than dominant.
- Retuned the vertical spacing between the shell header, the brand card, the main nav, and the action links, while also tightening the nav tone system so `세종AI 챗봇` stands out first, `박세종 | PR` reads second, and the dashboard sits third.
- Removed the separate `박세종 PR` nav item so the sidebar no longer duplicates the same root destination in both the brand card and the main nav.
- Reordered the creator PR layout so the strongest career-transition statement appears first, which should make the root page read more directly in the first screenful.
- Reduced hero CTA noise by removing the extra GitHub shortcut there and upgrading the Notion link into a clearer right-aligned portfolio button.
- Stripped the top-value button row out of the hero card to reduce clutter in the first viewport.
- Moved the portfolio action under the hero and brought the AI-generated note card up beside it, so the first screen now has one message card followed by two supporting proof cards.
- Dropped the hero subheadline line because the new title and intro already carry the message cleanly on their own.
- Softened the hero copy itself so the first sentence is easier to understand at a glance than the earlier `Founder-PM` framing.
- Shifted the Hupository CTA into the AI note card so the intro block reads more cleanly and the supporting proof cluster feels more intentional.
- Collapsed the AI note footer into a single row by placing the Codex signature beside the Hupository link.
- Dropped the standalone creator intro card because it had become redundant once the hero and support cards were doing the explanatory work.
- Adjusted the Notion badge styling to a black-on-white treatment and made it slightly larger for a more familiar brand feel.
- Dropped the separate project-direction card because it was one more explanatory stop than the tighter root-page flow needed.
- Dropped the creator timeline section from the root PR flow so the page stays focused on current fit and next-step proof.
- Tightened the hero card height by removing the unused bottom content area that was leaving extra whitespace in the creator view.
- Added a brief SDGs explainer sentence to make the section easier to understand for people unfamiliar with the acronym.
- Clarified the preferred frontend stack fit by explicitly mentioning `React Router v7` in the Korean teammate-role description.
- Added an SDG wheel-style shortcut in the section header that points to the official all-goals page, while guarding against remote image failures with a local fallback.
- Added back balanced inner padding to the creator hero after removing the empty content block, so the card keeps a cleaner but still comfortable rhythm.
- Softened the teammate-role naming by changing the Korean labels from mixed English-plus-`오너` wording into plain Korean role areas.
- Added a tiny health-check interaction in the creator hero so visitors can click once and see current backend response speed for fun.
- Shortened the frontend teammate-role copy by removing the more detailed card-flow implementation bullet.
- Turned the contact CTA into a clearer responsive split layout: message on the left, mail copy card on the right, stacked on mobile.
- Simplified the portfolio support card into a centered icon-title arrangement without the extra arrow, so the first proof row reads faster and feels calmer than the earlier badge-heavy versions.
- Rebuilt the SDGs area with a calmer hero-like gradient, a labeled official link card, and more readable goal cards so the section no longer feels visually mismatched.
- Flattened the SDGs description block and widened it a bit more so the copy feels less boxed in, while shortening the visible Korean link label for the official goals page.
- Simplified the SDGs footer note into one short sentence centered on Hupository-based AI inference, instead of a longer model-disclaimer paragraph.
- Simplified the MBTI footer note as well, replacing the longer self-report disclaimer copy with the same concise Hupository-based inference language.
- Updated the contact wording to sound more direct and current: if someone wants to team up, email is now the explicit next step and the reason for that channel is stated plainly.
- Replaced the old `Why now` card with a more forward-looking `Plans ahead` card centered on learning from mentors and experts, plus a short team-building-to-startup timeline.
- Refined that contact line again with a more natural Korean wording choice: `실무에서는` instead of `현업에서는`.
- Refined the `Plans ahead` wording again so it now expresses a stronger preference for teammates who value learning from mentors and understand the weight of even a short expert meeting.
- Refined the `Plans ahead` card yet again by turning the mentor-meeting value into the title itself and compressing the body copy into a shorter, more direct preference statement.
- Reframed the strengths/ownership card into `서포트형 리더십`, shifting the emphasis toward supportive role assignment, self-learning to cover missing functions, and practical experience from international camp PM and admissions consulting work.
- Nudged the three `Plans ahead` timeline bullets inward slightly so the date list feels less flush to the card edge.
- Applied the same inward bullet spacing to the supportive-leadership card so the paired creator cards feel more aligned.
- Renamed the left creator-card label from a timeline-like `Plans ahead` framing to `Preferred teammate type`, which better reflects that the card is really about teammate preference.
- Shortened the teammate-preference card title again so it now lands as a cleaner, faster-scanning phrase about valuing mentors.
- Broke the contact CTA headline into two lines and updated the renderer to respect that line break, giving the message a calmer rhythm inside the card.
- Retuned the main `AI Sejong` nav highlight from green into a subtle rose-red palette so it still feels featured without changing the overall layout language.
- Removed the dedicated chat-page intro card so the `AI Sejong` route opens straight into the functional chat surface without one more explanatory stop.
- Reworded the chat/Q&A surface to describe it as Hupository-based Sejong AI, which better matches how the system is actually positioned now.
- Added the same Hupository source-action to the dedicated chat route, aligned on the right above the chat surface for easy access.
- Pulled that source-action inside the actual Sejong AI card header so the page feels more self-contained and less like it has a detached utility button.
- Dropped the duplicated chat-description sentence and taught the card to simply omit empty description lines, keeping the header tighter.
- Added more truthful developer-facing copy to the chat `How it works` modal, including the actual YAML/README files and the fact that snippet selection is keyword-driven.
- Switched the logging notice styling from amber to rose so the persistence warning feels more explicit in both the chat card and the developer-note modal.
- Added one more developer-facing detail to the modal by naming the exact response model now in use: Anthropic Claude Haiku 4.5 (`claude-haiku-4-5-20251001`).
- Repointed the backend's default Hupository lookup to `apps/backend/hupository`, matching the newly moved in-repo dataset so deployments can bundle and read the same latest source without extra path wiring.
- Simplified the AI Sejong `How it works` modal again into four explanation cards, moved the model into a lighter footer line, and rewrote the copy so it now reflects the real current setup: roughly 400 KB of bundled backend Hupository data today, with GitHub-fetch or pgvector retrieval as the later upgrade path.
- Folded that future-upgrade note directly into the `참고 데이터` card and removed the path-heavy file list so the modal reads more like a product explanation than an implementation dump.
- Clarified the remaining two `How it works` cards again by mentioning session reset behavior in the multi-turn note and explicitly stating that saved chat logs are not deletable once written.
- Added a matching `회원가입` button under the Sejong AI login CTA and refactored the repeated logged-out actions into one shared block so both chat gate states stay aligned.
- Matched the chatbot gate's `로그인` button to the auth login page's primary button color, while demoting `회원가입` to an outline style so the two stacked actions keep a clear hierarchy.
- Moved that new `회원가입` CTA off the Sejong AI gate and into the actual `/auth/login` page instead, so the chat card only handles login while the auth page owns both entry actions.
- Retinted the new login-page `회원가입` CTA into a light blue accent so it stays secondary to `로그인` while still feeling more inviting than a plain neutral outline.
- Removed the extra explanatory sentence above that login-page sign-up CTA so the block now reads more cleanly as a single action.
- Removed the separate httpOnly-cookie explanation line from the login page header so the screen now gets to the actual auth inputs faster.
- Added a mobile auth-route auto-scroll so tapping `로그인` or `회원가입` from the sidebar now scrolls the content column into view, making the card change much more obvious under the persistent top nav.
- Expanded AI Sejong's grounding again so it now always reads `decisions.yaml` and the latest weekly `session.md`, while only pulling the latest daily journal when the question sounds like `today / lately / worries / thoughts`.
- Restored the missing `세션 초기화` action on the dedicated `/ai/sejong` chat page by letting the button render in the full creator-chat header, not only in the older compact variant.
- Increased the `decisions.yaml` grounding allowance substantially so the newer bottom entries, including the latest decision logs, no longer get cut off before reaching the model.
- Softened the two bottom chat warnings by replacing the old card-like note blocks with lighter colored text lines, so the footer reads as guidance instead of two more stacked panels.
- Moved the keyboard-send hint into the chat textarea placeholder itself and removed the separate bottom hint line, making the input area more self-explanatory with one less footer row.
- Reordered the remaining footer notes again so the red logging warning now sits at the absolute bottom, under the rate-limit line, where it reads more like a final policy notice.

### Why It Mattered

- The shared link now lands on the real PR page instead of a one-off landing wrapper.
- This removes one extra decision point and keeps the first impression tighter for fast video walkthroughs and Notion sharing.
- The AI can now answer more naturally about current direction, recent decisions, and active 고민 without opening the floodgates to every raw journal draft.
- The dedicated Sejong AI page regained an important control, so users can clearly start over without wondering whether session memory is still carrying over.
- Recent decision-making context is now actually available to the model instead of being silently truncated at the middle of the file.
- The chat footer now feels less boxy and repetitive because the low-priority notices no longer compete with the main chat surface as full cards.
- The chat composer now explains itself closer to the point of action, which keeps the bottom area shorter and easier to scan.
- The footer order now feels more deliberate because the hardest warning comes last instead of interrupting the softer guidance lines above it.
- The nav now better signals that Seoul is the currently active ops surface, while Busan exists only as a visible but not-yet-open expansion target.
- The shell now exposes Sejong's long-form writing archive more directly, which better supports the PR intent than a repeated explanatory sentence.
- The repository is now one click away from the same shell that already exposes contact and writing proof, which makes the builder credibility loop tighter.
- The action cluster now feels less like three unrelated buttons and more like one intentional proof/contact block.
- The sidebar should now feel calmer and more intentional overall because spacing and emphasis are working together instead of competing.
- The navigation should also feel cleaner now because the home destination exists only once, through the top brand card.
- The root page should now communicate Sejong's strongest positioning faster because the role-defining card is no longer buried under the shorter intro wrapper.

### Files To Read

- [router.tsx](../../apps/frontend/src/router.tsx)
- [App.tsx](../../apps/frontend/src/App.tsx)
- [common.json](../../apps/frontend/public/locales/ko/common.json)
- [common.json](../../apps/frontend/public/locales/en/common.json)

## 2. The Root Landing Was Rewritten For 30-Second Persuasion

### What Changed

- Rebuilt the root hero around three fast answers: who Sejong is, why he came to SoMa, and what kind of teammates he wants.
- Added a proof-first section that pulled YouTube, `AI Sejong`, `Creator PR`, and the Seoul dashboard forward as the key evidence bundle.
- Compressed the home page again so it behaved more like a short navigation surface for 300 prospective teammates instead of repeating the longer `Creator PR` story.
- Added an `#ai-sejong` anchor on the creator page so the root CTA could jump straight into the AI demo area.
- Later in the day, moved the creator cards fully into `Park Sejong PR`, split `AI Sejong` into its own `/ai/sejong` route, and finally removed the separate root landing because the PR page itself became the better shared entry.

### Why It Mattered

- The shared link is now much closer to a recruiting front door than a generic product landing.
- The page is now easier to scan during a video walkthrough because the root no longer tries to explain everything by itself.

### Files To Read

- [App.tsx](../../apps/frontend/src/App.tsx)
- [persona-page.tsx](../../apps/frontend/src/features/persona/pages/persona-page.tsx)

## 3. Share Metadata Was Brought In Line With The New Story

### What Changed

- Replaced the old `ASM 17 Community` browser/share metadata with `Park Sejong | Creator PR for AI SW Maestro 17`.
- Added a new root-focused OG image asset and connected both Open Graph and Twitter metadata to it.
- Rewrote the OG image copy in Korean so the shared card itself now matches the Korean-first share context.
- Synced the runtime root page metadata so the browser title/description stay aligned after the app loads.

### Why It Mattered

- Shared links now communicate the creator-first story instead of advertising only the community tool.
- The browser tab, unfurl text, and root hero all now tell the same story.

### Files To Read

- [index.html](../../apps/frontend/index.html)
- [og-image-sejong-pr.svg](../../apps/frontend/public/og-image-sejong-pr.svg)
- [App.tsx](../../apps/frontend/src/App.tsx)

## 4. Repo Docs Were Realigned To The New Front Door

### What Changed

- Rewrote the top of `README.md` so it now describes the project as `Park Sejong PR + ASM17 team-building tool`.
- Updated the current user flow, main local URLs, and demo paths to reflect `/` being the PR page itself and `/persona/sejong` remaining only as a legacy alias.
- Added this daily changelog file and linked it from the changelog index.

### Why It Mattered

- The repo authority document now matches the product hierarchy shown in the app.
- Future work should be easier because contributors can read one clear current-state story instead of piecing it together from yesterday's pivot.

### Files To Read

- [README.md](../../README.md)
- [README.md](../../docs/changelog/README.md)
- [2026-03-25.md](../../docs/changelog/2026-03-25.md)

## 5. Backend Hupository Defaults Now Match The In-Repo Data Folder

### What Changed

- Updated the backend persona service so its default Hupository search order now checks `apps/backend/hupository` before the older root-level fallback paths.
- Added `HUPOSITORY_ROOT` to the backend env example as an optional override, while documenting that the backend in-repo folder is now the default place for the bundled data.
- Clarified the runtime docs so contributors know the current deploy-friendly location for the Hupository snapshot.

### Why It Mattered

- The user had already moved the latest Hupository snapshot into the backend app directory, but the old lookup logic still preferred legacy root-level locations.
- This change lets local runs and Docker-style backend packaging read the same in-repo dataset by default, with less hidden path knowledge.

### Files To Read

- [service.py](../../apps/backend/app/features/persona/service.py)
- [.env.example](../../apps/backend/.env.example)
- [README.md](../../README.md)

## 6. The AI Sejong Dev Note Was Simplified Around The Real Current Architecture

### What Changed

- Reduced the `How it works` modal from five cards down to four by removing the standalone model card.
- Moved the model name into a lighter footer line at the bottom of the modal instead of giving it equal visual weight with the operational explanations.
- Rewrote the Korean and English helper copy so it now describes the present structure more truthfully: the server currently reads about 400 KB of bundled Hupository data from `apps/backend/hupository`.
- Added a short roadmap note explaining that the likely next retrieval upgrade is either direct GitHub fetches or pgvector-based retrieval.
- Later folded that upgrade note into the `Grounding data` card itself and removed the noisier path-level file list so the section reads more clearly for non-developers.
- Refined the remaining operational copy again so the multi-turn explanation now mentions session reset behavior, and the logging card now states plainly that chat logs are not deletable once saved.
- Nudged the lightweight model line above the red logging warning card so the modal footer now reads in a calmer order: model first, irreversible logging warning second.
- Expanded the backend retrieval layer so the chat now includes `data/08_now/decisions.yaml` and the latest weekly `session.md` by default, with the latest dated journal note added only for `today / recent / worry / thought` style questions.
- Fixed a small frontend regression where the session-reset button had disappeared from the dedicated creator chat route after the header actions were refactored.
- Relaxed the `decisions.yaml` snippet limit from `1800` to `4200` characters so the file can currently be delivered almost in full.
- Replaced the chat card's bottom `SectionNote` disclaimer/logging panels with plain tinted text lines for a lighter visual weight.
- Removed the separate `hint` line from the chat footer and folded that keyboard guidance into the localized placeholder text instead.
- Moved the plain red `loggingNotice` line below `rateLimitHint` so it is the last footer element in the chat card.
- Folded the rate-limit hint into the placeholder too and removed the separate footer line, so the composer now carries both the keyboard and quota guidance by itself.
- Added a manual line break inside the localized placeholder so the chat textarea can show that guidance across two lines instead of one dense sentence.
- Wrapped the keyboard-send hint inside parentheses in the placeholder so the first line reads more naturally as one sentence before the quota note below.
- Reworked the shared `Textarea` so it can start at a specified row count and grow with the content, then applied that to the Sejong AI composer with a three-line default height.
- Extended the persona chat API responses with a tiny quota payload so the frontend now knows the remaining hourly question count and the next reset time without guessing.
- Added a lightweight bottom-right quota label in the Sejong AI composer footer so users can see `questions left + reset time` at a glance.
- Restored the `How it works` button in the logged-in dedicated Sejong AI chat header instead of showing it only on the compact or pre-login variants.
- Expanded the `Access` note in the `How it works` modal so it now explains that the 10-question limit is managed with a rolling one-hour window, not a single fixed reset.
- Tightened the copy across all four `How it works` cards so the modal reads faster, while also adding the `10 questions per hour` note directly into the multi-turn card as well.
- Corrected the multi-turn note again so it now matches the actual implementation: the model sees only the latest 12 chat messages from the current session, while the hourly 10-question cap stays an access rule instead of a memory rule.
- Added one extra blank placeholder line between the keyboard hint and the hourly-question note so the two pieces of guidance breathe a bit more inside the taller textarea.

### Why It Mattered

- The older wording still implied a GitHub-local mirror story that had already become confusing after the Hupository snapshot moved into the backend app.
- The modal now matches the product's actual implementation more closely while also signaling a plausible future architecture that developers will recognize immediately.
- First-time visitors can now start from the AI gate without backing out to the global nav just to find the sign-up path.
- The logged-out AI gate now feels more consistent with the actual auth flow because the primary action visually matches the dedicated login screen.
- The login page now becomes the single place where both auth entry choices live, which keeps the chat gate simpler and makes the user's intended flow more obvious.
- The sign-up path now has a softer visual identity of its own without competing with the primary login action.
- The login screen now feels less wordy because the sign-up action can stand on its own without one more helper sentence.
- The login entry now feels more direct because it no longer opens with infrastructure detail before the actual form.
- Mobile navigation into auth now feels more intentional because the user is taken straight to the changed card instead of staying visually stuck near the sidebar.
- The AI grounding stays richer without becoming too noisy because structured `decisions` and cleaned weekly summaries are always present, while rawer day notes stay conditional and `session_draft.md` remains excluded.
- The dedicated chat controls now better match user expectations again because reset is visible alongside the other top-level chat actions.
- The latest decision entries now survive grounding, so questions about the current front-door strategy are much less likely to miss the most recent context.
- The lowest-priority notices now stay visible without visually overbuilding the bottom of the chat card.
- The input area now carries its own send/newline guidance, so the footer spends less space repeating interaction instructions.
- The chat composer now feels less cramped at first glance while still staying flexible for longer multi-line questions.
- The quota is now more transparent because users can see the remaining allowance before they hit the hard 429 wall.
- The footer stays lightweight because the quota note is just a small status line instead of a new card or modal.
- Logged-in users now keep access to the same explainer entry point, so the chat no longer becomes less understandable after authentication.
- The question-cap rule is less likely to confuse people now that the modal explains the `oldest question expires first` behavior directly.
- The explainer now stays easier to scan because each card is shorter and the quota rule is visible both in access policy and in the memory summary.
- The modal is now more trustworthy again because the multi-turn card no longer mixes up `memory window` and `rate limit` as if they were the same mechanism.
- The placeholder now feels calmer because the interaction hint and the quota hint are visually separated instead of touching as two dense lines.
- The red logging notice now lands as the final cautionary note, which better matches its seriousness without adding more visual weight.
- The footer is now even shorter because both interaction help and question-limit guidance live directly in the textarea placeholder.
- The composer hint should now feel easier to scan because the same information is split into two lines inside the input instead of running as one long placeholder.
- The placeholder copy now reads a bit more cleanly because the keyboard guidance is visually grouped as a parenthetical aside.

### Files To Read

- [persona-page.tsx](../../apps/frontend/src/features/persona/pages/persona-page.tsx)
- [persona.json](../../apps/frontend/public/locales/ko/persona.json)
- [persona.json](../../apps/frontend/public/locales/en/persona.json)
- [service.py](../../apps/backend/app/features/persona/service.py)

## End-of-Day Result

- The most important change is that the root link and `Park Sejong PR` are now the same entry surface.
- `Park Sejong PR`, `AI Sejong`, and the dashboard still all matter, but they now sit in a deliberate order that better matches a time-sensitive team-building share context.
